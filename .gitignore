node_modules/
dist/
dist-pwa/
build/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
.expo/
android/
ios/
*.apk
*.aab
*.ipa
.vscode/settings.json

# Client / customer data — this repo is Tier 3 (storefront only, no client data).
# Never let a real client brief, name, or business data land in this repo.
# If Package B work needs a workspace, use a separate repo per engagement (see docs/GROWTH.md).
clients/
**/customer-data/
**/*-secret*
*.pem
*.key
credentials.*
