# Architecture — [PROJECT NAME]

Defines the tech stack, folder structure, and key architectural decisions.
Fill during pre-production. Can be generated by Idea Forge (Architecture stage) or filled manually.
Lock before Phase 1 code begins.

---

## Tech Stack

| Layer | Choice | Rationale |
|---|---|---|
| Language | | |
| Framework / Engine | | |
| UI | | |
| State management | | |
| Data storage | | |
| Backend / API | | |
| Build tool | | |
| Test framework | | |

**Stack decision logged in:** `docs/DECISIONS.md` as D00X

---

## Folder Structure

```
[project-root]/
├── [folder]/          # [purpose]
├── [folder]/          # [purpose]
│   ├── [subfolder]/   # [purpose]
│   └── [subfolder]/   # [purpose]
└── [folder]/          # [purpose]
```

---

## Key Architectural Rules

Rules that govern how the codebase is built. Enforced by all AI tools.

1. **File size cap:** [e.g. 300 lines per file — split if exceeded]
2. **Module responsibility:** [e.g. one concern per file]
3. **State access:** [e.g. no direct state mutation from UI components]
4. **Data persistence:** [e.g. soft delete only, never hard delete]
5. **API key handling:** [e.g. server-side only, never in client code]
6. [Add more as decided]

---

## Architecture Decision Records (ADRs)

| # | Decision | Rationale | Date |
|---|---|---|---|
| ADR-001 | [e.g. Use Godot over Unity] | [reason] | [YYYY-MM-DD] |
| ADR-002 | | | |

Full decisions in `docs/DECISIONS.md`.

---

## Integration Points

Systems or services this project depends on:

| System | Purpose | Notes |
|---|---|---|
| [e.g. Supabase] | [purpose] | [API key location, auth method] |
| | | |

---

## Known Technical Risks

| Risk | Likelihood | Mitigation |
|---|---|---|
| [e.g. target device memory cap] | High | [specific mitigation] |
| | | |

---

*Source: [Idea Forge Architecture export YYYY-MM-DD / Manual]*
*Architecture status: Draft → Review → Locked*
*Locked: [YYYY-MM-DD]*
