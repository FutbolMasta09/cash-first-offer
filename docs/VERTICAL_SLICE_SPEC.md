# Vertical Slice Spec — [PROJECT NAME]

The vertical slice is the smallest playable/testable proof that the core loop works.
Fill this before any Phase 1 code. Can be generated by Idea Forge (Slice Spec stage) or filled manually.

---

## Slice Definition

**Start →** [What is the entry point? Where does the player/user begin?]
**Core loop →** [What are the 2–3 steps of the core loop in the slice?]
**End condition →** [What signals the slice is "complete"? How does it end?]

---

## In Scope for Vertical Slice

List only what is explicitly included. Be specific and stingy.

- [ ]
- [ ]
- [ ]

---

## Explicitly Out of Scope

List what is tempting but excluded. Prevents scope creep.

- [Feature or content]
- [Feature or content]
- [Feature or content]

---

## Slice Success Criteria

The slice is "done" only when ALL of these are true:

1. [Criterion — functional: e.g. "core loop runs without crashes"]
2. [Criterion — feel: e.g. "the loop feels like gameplay, not admin work"]
3. [Criterion — hook: e.g. "a new player understands what to do without explanation"]
4. [Criterion — device: e.g. "runs on target device at acceptable performance"]

---

## Top Failure Modes

What are the 2–3 ways this slice could fail to prove the concept?

1. [Failure mode + early warning signal]
2. [Failure mode + early warning signal]
3. [Failure mode + early warning signal]

---

## Slice Scope — Platform / Performance Targets

| Target | Requirement |
|---|---|
| Device | [Target device for slice test] |
| FPS / performance | [Minimum acceptable] |
| Load time | [Acceptable range] |
| Memory | [Cap if relevant] |

---

## Go/No-Go After Slice

After the slice is playable, run this evaluation:

| Question | Pass criteria | Pass? |
|---|---|---|
| Does the core loop feel like [intended experience]? | [describe what "yes" looks like] | |
| Did failure modes stay away? | None of the 3 failure modes occurred | |
| Technical feasibility confirmed? | No fundamental blockers discovered | |
| Ready to build Phase 1 at full scope? | | |

**Go/No-Go decision logged in:** `docs/DECISIONS.md` as D00X
**Decision date:** [YYYY-MM-DD]

---

*Source: [Idea Forge Slice Spec export YYYY-MM-DD / Manual]*
*Spec status: Draft → Review → Locked*
*Locked: [YYYY-MM-DD]*
